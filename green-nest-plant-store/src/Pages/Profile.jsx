import React from 'react';
import Header from '../Components/Header';

const Profile = () => {
    return (
        <div>
            <title>Profile</title>
            <Header></Header>
            Profile Page
        </div>
    );
};

export default Profile;